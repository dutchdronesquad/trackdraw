"use client";

export {
  clipboard,
  clamp,
  isTypingInInput,
  mergeClientRects,
  MIN_MARQUEE_SIZE,
  normalizeRect,
  rectsIntersect,
} from "@/lib/canvas/shared";

export type { CursorState, DraftPoint, RectLike } from "@/lib/canvas/shared";
